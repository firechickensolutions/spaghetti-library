# dev skill
- **dev** (`skills/dev/SKILL.md`) - code generation pattern library, 19 entries across SQL, Python, TypeScript, React, PowerShell, and auth
- At the start of any task that involves writing code, invoke the Skill tool with `skill: "dev"` before writing. Check the trigger table. When a trigger fires, halt and read the entry before generating.
